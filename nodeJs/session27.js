//primitive data
//all data is saved in memory as a binary

/* 
    primitives:
    1. undefined
    2. null
    3. string
    4. number
    5. bigint
    6. boolean
*/
let number1 = null;

let number2;
console.log(number1 == number2); //two == signs means only compares the values
console.log(number1 === number2); // three === equals compares the value and data type
